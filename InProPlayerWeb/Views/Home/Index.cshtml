<h3>@ViewData["Title"]</h3>
<div class="row">
    @{
        for (int i = 1; i <= int.Parse(ViewData["groupCount"].ToString()); i++)
        {
            <div class="group col-lg-2 col-md-4 col-sm-6 col-xs-12">
                <div class="group-title">
                    群組 @i
                </div>
                <button type="button" class="turnBtn btn btn-outline-primary bi bi-toggle2-on" data-type="group" data-num="@i">
                    開啟
                </button>
            </div>
        }
    }
</div>
<hr>
<div class="row">
    @{
        for (int i = 1; i <= int.Parse(ViewData["areaCount"].ToString()); i++)
        {
            <div class="area col-lg-2 col-md-4 col-sm-6 col-xs-12">
                <div class="area-title">
                    @i.ToString().PadLeft(3, '0')
                </div>
                <button type="button" class="turnBtn btn btn-outline-primary bi bi-toggle2-on" data-type="area" data-num="@i">
                    &nbsp;開啟
                </button>
            </div>
        }
    }
</div>