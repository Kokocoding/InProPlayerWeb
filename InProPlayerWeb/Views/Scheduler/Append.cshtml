<form method="post" action="/Scheduler/Append">
    <div class="mb-3 row">
        <label for="title" class="form-label col-sm-2 col-form-label text-center">名稱</label>
        <div class="col-sm-10">
            <input type="text" class="form-control" id="title" name="SchedulerName" required>
        </div>
    </div>
    <div class="mb-3 row">
        <label for="GroupID" class="form-label col-sm-2 col-form-label text-center">群組</label>
        <div class="col-sm-10">
            <select class="form-select" name="GroupID">
                @foreach (var data in ViewBag.GroupSelect)
                {
                    <option value="@data.id">@data.title</option>        
                }
            </select>
        </div>
    </div>
    <div class="mb-3 row">
        <label for="week" class="form-label col-sm-2 col-form-label text-center">定時星期</label>
        <div class="col-sm-10">
            <input type="checkbox" class="form-check-input" name="week[]" value="*" id="flexCheckChecked-*">
            <label class="form-check-label" for="flexCheckChecked-*">
                每日　
            </label>
            @for(int i = 1; i <= 7; i++)
            {
                <input type="checkbox" class="form-check-input" name="week[]" value="@i" id="flexCheckChecked-@i">
                <label class="form-check-label" for="flexCheckChecked-@i">
                    @ViewBag.WeekDict[@i.ToString()]　
                </label>
            }
        </div>
    </div>
    <div class="mb-3 row">
        <label for="time" class="form-label col-sm-2 col-form-label text-center">定時日期</label>
        <div class="col-sm-10">
            <input type="time" class="form-control form-time" name="time" step="1" pattern="([01]?[0-9]|2[0-3]):[0-5][0-9]:[0-5][0-9]" />
        </div>
    </div>
    <div class="mb-3 row">
        <label for="LoopType" class="form-label col-sm-2 col-form-label text-center">播放模式</label>
        <div class="col-sm-10">
            <select name="LoopType" class="form-select">
                <option value=""></option>
                <option value="1">播放次數</option>
                <option value="2">播放秒數</option>
            </select>
        </div>
    </div>
    <div class="mb-3 row LoopType1 LoopTypeNone">
        <label for="LoopTimes" class="form-label col-sm-2 col-form-label text-center">播放次數</label>
        <div class="col-sm-10">
            <input type="text" class="form-control" name="LoopTimes" placeholder="單位次 ex.3次請輸入 3">
        </div>
    </div>
    <div class="mb-3 row LoopType2 LoopTypeNone">
        <label for="KeepTimes" class="form-label col-sm-2 col-form-label text-center">播放秒數</label>
        <div class="col-sm-10 ">
            <input type="text" class="form-control col-sm-10" name="KeepTimes" placeholder="單位秒 ex.120秒請輸入 120">
        </div>
    </div>
    <div class="text-center">
        <button type="submit" class="btn btn-primary">送出</button>
        <button type="button" class="btn btn-primary" onclick="history.back();">取消</button>
    </div>
</form>
<script src="~/js/scheduler.js"></script>